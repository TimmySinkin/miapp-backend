spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d9c7pcgk1i2s73ari0l0-a.oregon-postgres.render.com:5432/miapp_db_h7ek}
spring.datasource.username=${DATABASE_USERNAME:miapp_db_h7ek_user}
spring.datasource.password=${DATABASE_PASSWORD:J1TDxQvojeGjdQ7zOwXgpiSGUJb6BUGi}

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# --- SMTP (Gmail) для отправки кода подтверждения email при регистрации ---
#
# Замените значения после двоеточия на свои (ваш Gmail и App Password),
# либо задайте переменные окружения MAIL_USERNAME / MAIL_PASSWORD перед
# запуском сервера — тогда именно они будут использованы, а не заглушка.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:tumofejsunkun123@gmail.com}
spring.mail.password=${MAIL_PASSWORD:jjnz fzfa eosv xtus}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Имя отправителя, которое видит получатель вместо email-адреса
# (например "MiniApp <ваша-почта@gmail.com>" вместо просто адреса).
# Сам адрес отправки при этом не меняется — Gmail не позволяет слать
# с чужого домена (типа noreply@sender.ozon.ru) без верификации домена в
# Google Workspace, поэтому реальный адрес всегда будет вашим Gmail-ящиком,
# меняется только видимое имя.
app.mail.sender-name=MiniApp

# --- Telegram Login Widget ---
# Токен вашего бота от @BotFather (тот, что вы уже получили).
# Используется ТОЛЬКО на бэкенде для проверки подписи данных от Telegram —
# никогда не передавайте его на фронтенд.
telegram.bot.token=${TELEGRAM_BOT_TOKEN:8814230092:AAGEMpjp2bWIdYVJVrHwIFVkbIL3j82t7zM}


yandex.storage.endpoint=https://storage.yandexcloud.net
yandex.storage.region=ru-central1
yandex.storage.bucket=${YANDEX_BUCKET:miapp-backend-app}
yandex.storage.access-key=${YANDEX_ACCESS_KEY:YCAJE5QmlHhhlr_uVr6spcYn6}
yandex.storage.secret-key=${YANDEX_SECRET_KEY:YCOwIRb_s5Gb5BzjIjs9wv1HxijWrTDshGcghqpZ}

server.port=${PORT:8080}

